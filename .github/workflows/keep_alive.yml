name: Keep Alive
on:
  schedule:
      - cron: '*/15 * * * *'
        workflow_dispatch:
        jobs:
          ping:
              runs-on: ubuntu-latest
                  steps:
                        - name: Ping
                                run: curl -s https://support-ticket-simulations.streamlit.app/
                                
